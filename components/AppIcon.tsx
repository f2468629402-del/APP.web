export function AppIcon({ name, color, accent, size = 'md' }: { name: string, color: string, accent: string, size?: 'sm'|'md'|'lg' }) { const cls = size === 'lg' ? 'h-24 w-24 rounded-[27px] text-4xl' : size === 'sm' ? 'h-12 w-12 rounded-[14px] text-xl' : 'h-16 w-16 rounded-[19px] text-2xl'; return <div style={{ backgroundColor: color, color: accent }} className={`${cls} flex items-center justify-center font-medium shadow-inner`}><span className="serif italic">{name[0]}</span></div> }
