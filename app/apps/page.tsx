import apps from '@/data/apps.json'
import Link from 'next/link'
import { AppIcon } from '@/components/AppIcon'
import { PhonePreview } from '@/components/PhonePreview'
import { FadeIn } from '@/components/FadeIn'
export default function AppsPage() { return <main className="mx-auto max-w-7xl px-6 pb-24 pt-36 lg:px-10"><FadeIn><p className="text-xs font-semibold uppercase tracking-[.28em] text-[#77736b]">The collection</p><h1 className="mt-5 text-6xl tracking-[-.06em] sm:text-8xl">All <span className="serif italic">apps.</span></h1><p className="mt-8 max-w-md text-[#77736b]">一组独立、专注而有温度的应用。每一个都从一个简单的问题开始。</p></FadeIn><div className="mt-24 grid gap-20 md:grid-cols-2">{apps.map((app, i) => <FadeIn key={app.slug} delay={i * .08}><Link href={`/apps/${app.slug}`} className="group block"><div className="relative flex min-h-[500px] items-center justify-center overflow-hidden rounded-[28px]" style={{ backgroundColor: app.color }}><div className="absolute left-8 top-8 text-xs uppercase tracking-[.2em] opacity-60">0{i + 1}</div><PhonePreview app={app}/><span className="absolute bottom-7 right-8 text-xl opacity-0 transition group-hover:opacity-100">↗</span></div><div className="mt-6 flex items-start justify-between"><div><div className="flex items-center gap-3"><AppIcon name={app.name} color={app.color} accent={app.accent} size="sm"/><h2 className="text-3xl tracking-[-.04em]">{app.name}</h2></div><p className="mt-4 max-w-sm text-sm leading-6 text-[#77736b]">{app.description}</p></div><span className="mt-2 text-xs text-[#77736b]">{app.category}</span></div></Link></FadeIn>)}</div></main> }
